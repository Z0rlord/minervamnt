# Rust
/target/
**/*.rs.bk
*.pdb

# Secrets & local env — never commit
.env
.env.*
!.env.example
.envrc
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
credentials.json
secrets.toml
.doppler/

# Internal operator config (use local copy; not for public repo)
doppler.yaml
config.signet.toml

# Databases & runtime data
/data/
*.sqlite
*.sqlite3
*.db
*.db-*

# Cloud / deploy tooling caches
.wrangler/
.cloudflared/
*.tunnel.json

# OS & editors
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log

# Local caches / tool installs (never commit)
.cache/
.local/

# Ad-hoc Pi scan scratch
.pi-scan*
.run-pi-scan.sh

# Local config backups
config.signet.toml.bak*
*.toml.bak-e2e
